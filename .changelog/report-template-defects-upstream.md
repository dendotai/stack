- **Report template defects upstream** (`CLAUDE.md`): a new top section tells an
  agent working in a downstream project which parts of the repo are template
  code, to search `dendotai/stack` and file what it finds there, and to keep
  every private detail out of that issue. A defect found downstream is a defect
  in every project built from the template. To apply: copy the first section of
  `CLAUDE.md`.

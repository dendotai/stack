# Written by `convex dev` on first run — this is just a reference of the shape.
# Links the local CLI to your project's DEV deployment. Do not commit the real one.
CONVEX_DEPLOYMENT=dev:your-dev-deployment # team: <team>, project: <project>

CONVEX_URL=https://your-dev-deployment.convex.cloud

CONVEX_SITE_URL=https://your-dev-deployment.convex.site
